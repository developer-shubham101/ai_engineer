# requirements-updated.txt — updated, compatible pins (keeps LangChain 0.0.x ecosystem)

# --- Web / server ---
fastapi==0.122.0
uvicorn[standard]==0.38.0
python-dotenv==1.0.0
pydantic==1.10.13           # keep pydantic v1 for langchain 0.0.x compatibility

# --- LangChain 0.0.x ecosystem (KEEP these exact pins) ---
langchain==0.0.330
langchain-core==0.1.23
langchain-community==0.0.20
langchain-google-genai==0.0.9
langsmith==0.0.87

# --- AI & local models ---
# Use a torch build known to be available on Windows/Python 3.10/3.11
torch==2.9.1               # available on PyPI for Windows; compatible with transformers. :contentReference[oaicite:2]{index=2}
transformers==4.57.3       # recent stable transformers release. :contentReference[oaicite:3]{index=3}
sentencepiece==0.2.1

# Embedding support
sentence-transformers==5.1.2  # latest stable major release. :contentReference[oaicite:4]{index=4}

# --- Vector DB / RAG ---
chromadb==0.4.24           # keep older chroma used in this codebase (safe for LC 0.0.x)
pypdf==6.4.0               # newer pypdf release. :contentReference[oaicite:5]{index=5}
tiktoken==0.12.0           # recent tiktoken release. :contentReference[oaicite:6]{index=6}

# --- External APIs & utilities ---
requests==2.31.0
openai==2.8.1              # latest official openai client release. :contentReference[oaicite:7]{index=7}
python-multipart==0.0.6

# Optional / native acceleration & dev tools
llama-cpp-python==0.1.54   # example pin (no exact requirement in repo); adjust if needed
rich==13.6.0               # pretty printing; choose a recent stable release

# --- Text Parsing ---
markdown==3.5.2
beautifulsoup4==4.12.3

# --- Authentication & Security ---
pyjwt==2.8.0
passlib[bcrypt]==1.7.4
